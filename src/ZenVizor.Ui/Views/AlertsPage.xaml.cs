using System.Windows.Controls;

namespace ZenVizor.Ui.Views;

/// <summary>
/// Alerts page. Brief §18a interim placeholder ships in the polish
/// interlude; the full Phase 6 functional layout (heterogeneous feed,
/// filter bar, dismiss flow, live arrival cues) replaces this when the
/// alert pipeline IPC + producer + storage land.
/// </summary>
public partial class AlertsPage : Page
{
    public AlertsPage()
    {
        InitializeComponent();
    }
}
